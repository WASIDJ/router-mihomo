# 路由器 SSH 登录目标 (用户名@IP)
TARGET="RSI@192.168.50.1"

# 机场订阅 URL (Clash/Mihomo 格式，可留空。若提供，build 时将自动拉取最新节点与规则)
AIRPORT_URL=""

# 自建节点配置文件路径 (默认 private/custom_nodes.yaml)
CUSTOM_NODES_FILE="private/custom_nodes.yaml"

# 路由器 LAN 配置
LAN_IP="192.168.50.1"
LAN_SUBNET="192.168.50.0/24"
